-- Show difference occured in a file 
  git log --graph --pretty=format:'%h%s' --patch -- SpotifyDownloader/settings.py

-- Show all the commits that occured in main
  git log --oneline main

-- Show all commits that changed the specified file
  git log --follow --name-status --graph --oneline -p -- SpotifyDownloader/settings.py

-- Restore a file to an earlier commit state
  git restore --source=7350fba --staged --worktree SpotifyDownloader/settings.py

-- For selectively revert parts of the file
  git checkout -p 7350fba -- SpotifyDownloader/settings.py

-- Git stash commands
  git stash	
  git stash list
  git stash apply  
  git stash pop
  git stash drop
